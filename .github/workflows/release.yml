name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 9.0.x

      - name: Test
        run: dotnet test tests/Jellyfin.Plugin.SponsorBlockSegments.Tests

      - name: Build
        run: dotnet publish src/Jellyfin.Plugin.SponsorBlockSegments -c Release -o publish

      - name: Package
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          mkdir package
          cp publish/Jellyfin.Plugin.SponsorBlockSegments.dll package/
          cp meta.json package/
          (cd package && zip "../sponsorblock-segments_${VERSION}.zip" ./*)
          echo "MD5 checksum for manifest.json:"
          md5sum "sponsorblock-segments_${VERSION}.zip"

      - name: Create GitHub release
        uses: softprops/action-gh-release@v3
        with:
          files: sponsorblock-segments_*.zip
          generate_release_notes: true
